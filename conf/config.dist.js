module.exports = {
    // Server port (number between 1 and 65535)
    port: 3000,
    // Server IP or domain (or 'localhost')
    domain: 'localhost',
    // Logging level (1, 2, 3, or 4)
    logLevel: 1
}